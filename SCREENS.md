# 画面設計・画面要件

親ドキュメント: [REQUIREMENTS.md](REQUIREMENTS.md)

## 画面構成
本アプリは単一画面(ボード画面)のみで構成される。

```
┌─────────────────────────────────────────────┐
│ ヘッダー: 「タスクボード」                     │
├─────────────────────────────────────────────┤
│ ┌─────────┐ ┌─────────┐ ┌─────────┐          │
│ │ リストA  │ │ リストB  │ │ リストC  │  …      │
│ │ (件数)   │ │ (件数)   │ │ (件数)   │          │
│ ├─────────┤ ├─────────┤ ├─────────┤          │
│ │ [カード] │ │ [カード] │ │ [カード] │          │
│ │ [カード] │ │ [カード] │ │          │          │
│ │+カード追加│ │+カード追加│ │+カード追加│          │
│ └─────────┘ └─────────┘ └─────────┘          │
├─────────────────────────────────────────────┤
│ [+ リストを追加]                              │
└─────────────────────────────────────────────┘
```

## 画面要素と操作

### ヘッダー
- アプリタイトルを表示するのみ(操作なし)

### リスト
| 要素 | 説明 |
|---|---|
| ドラッグハンドル(⠿) | 掴んでドラッグするとリストの並び順を変更 |
| タイトル(入力欄) | クリックして直接編集可能。編集確定でトーストなどはなく即保存 |
| 件数表示 | リスト内のカード枚数を自動表示 |
| 削除ボタン(×) | クリックで確認ダイアログ後、リストごと削除 |
| カード一覧 | カードを縦に並べて表示。ドラッグ&ドロップの受け皿になる |
| 「+ カードを追加」ボタン | クリックでカード入力フォームを表示 |

### カード
| 要素 | 説明 |
|---|---|
| チェックボックス | 完了/未完了を切り替え。完了時はテキストに取り消し線 |
| テキスト | クリックでその場編集(テキストエリアに切り替わる)。Enterで確定、Escでキャンセル |
| 削除ボタン(×) | クリックで即削除(確認なし) |
| カード本体 | ドラッグしてリスト間・リスト内を移動可能 |

### リスト追加
- 画面下部の「+ リストを追加」ボタンから、名前を入力して追加

## 画面要件(表示ルール)
- リストは横並びで、画面幅を超える場合は横スクロール
- カードが増えた場合はリスト内で縦に増えていく(現状は縦スクロール制御なし)
- 空のリスト・空のボードでもエラーにならず、操作可能な状態を維持する
