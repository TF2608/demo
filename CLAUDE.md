# 開発ルール（GitHub運用）

このリポジトリでの作業は、以下のルールを厳密に守ること。ユーザーから直接的な作業指示（コード変更・機能追加・バグ修正など）を受けた場合、Claude Codeは必ずこのフローに従う。

## 1. 作業前に必ずIssueを作成する

- コードを変更する作業に着手する前に、対応するGitHub Issueが存在するか確認する。
- 存在しない場合は `gh issue create` で先にIssueを作成してから作業を始める。
- Issueには概要・背景・対応方針を記載する（`.github/ISSUE_TEMPLATE/` のテンプレートを使用）。
- ドキュメント修正やタイポ修正など、ごく軽微な変更であってもIssueなしでmainに直接コミットしてはならない（後述のブランチ運用に従う）。

## 2. mainブランチに直接コミット・プッシュしない

- `main` ブランチで直接編集・コミット・`git push origin main` を行うことは禁止。
- 作業は必ずIssueに対応する作業ブランチを作成して行う。

### ブランチ命名規則

Issue番号と種別を含める。

- 新機能: `feature/#<issue番号>-概要` （例: `feature/12-add-login-api`）
- バグ修正: `fix/#<issue番号>-概要` （例: `fix/15-fix-drag-bug`）
- 雑務・整備: `chore/#<issue番号>-概要` （例: `chore/20-update-deps`）

## 3. 変更は必ずPull Request経由でmainにマージする

- 作業ブランチでの変更後、`gh pr create` でPull Requestを作成する。
- PR本文には対応するIssueを `Closes #<issue番号>` の形式で記載し、マージ時にIssueが自動クローズされるようにする（`.github/PULL_REQUEST_TEMPLATE.md` を使用）。
- mainブランチはGitHub側のブランチ保護ルールにより直接pushが拒否される設定になっている。PRを経由せずにマージすることはできない。

## 4. コミットメッセージ

- 対応するIssue番号を参照する（例: `#12 ログインAPIを追加`）。

## 5. Claude Codeの振る舞い

- ユーザーから作業指示を受けたら、まずIssueの有無を確認し、なければ作成する。
- 次に上記命名規則に従った作業ブランチを作成してから実装に着手する。
- 実装完了後はPull Requestの作成まで行う（ユーザーが明示的にマージまで指示した場合を除き、マージはユーザーの承認を得てから行う）。

## 6. 開発サーバー起動時のポート運用ルール

動作確認のためにバックエンド・フロントエンドの開発サーバーを起動する際は、以下を厳守する。

- 既定ポートは固定とし、必ずこのポートで起動する。別の空きポートへのフォールバック起動は禁止。
  - バックエンド（Spring Boot）: `8080`
  - フロントエンド（Vite）: `5173`
- 起動しようとしたポートが既に使用中の場合、そのポートで待受しているプロセスを確認する（例: `lsof -nP -iTCP:<port> -sTCP:LISTEN`）。
- 確認したプロセスのコマンド名・PIDをユーザーに提示した上で、そのプロセスを停止（`kill`）してから、既定ポートで起動し直す。無関係なプロセスを誤って停止しないよう、提示なしに停止しない。
- フロントエンドの `VITE_API_BASE_URL`（既定 `http://localhost:8080`）およびバックエンドのCORS許可オリジン（既定 `http://localhost:5173`）は、いずれも上記の既定ポートを前提としている。別ポートで起動すると通信できなくなるため、既定ポート以外での起動は行わない。
